/**
 * Call Screen - Web
 * Web calls are fully supported through Agora Web SDK (lib/agora/webCallClient.ts).
 * This file just re-exports the main call screen, which has web-specific branches.
 */
export { default } from "./call";
